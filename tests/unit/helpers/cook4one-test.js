import { cook4one } from 'cook4one/helpers/cook4one';
import { module, test } from 'qunit';

module('Unit | Helper | cook4one');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cook4one([42]);
  assert.ok(result);
});
